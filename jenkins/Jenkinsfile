pipeline {
    agent {
        docker {
            image 'node:6-alpine'
            args '-p 3000:3000'
        }
    }
    environment {
        CI = 'true'
    }
    stages {
        stage('Build') {
            steps {
                sh 'echo "npm install"'
            }
        }
        stage('Test') {
            steps {
                sh 'ls -la ./jenkins/scripts/test.sh'
            }
        }
        stage('Deliver') {
            steps {
                sh 'ls -la ./jenkins/scripts/deliver.sh'
                input message: 'Finished using the web site? (Click "Proceed" to continue)'
                sh ' ls -la ./jenkins/scripts/kill.sh'
            }
        }
    }
}
